apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "nesthelm.fullname" . }}
  labels:
    {{- include "nesthelm.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "nesthelm.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "nesthelm.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 3000
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
